//tabby.onFirefoxLoad = function(event) {
  //document.getElementById("contentAreaContextMenu")
          //.addEventListener("popupshowing", function (e){ tabby.showFirefoxContextMenu(e); }, false);
//};

//tabby.showFirefoxContextMenu = function(event) {
  //// show or hide the menuitem based on what the context menu is on
  //document.getElementById("context-tabby").hidden = gContextMenu.onImage;
//};

//window.addEventListener("load", tabby.onFirefoxLoad, false);
